{
  "name": "kun-lang",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "docs:dev": "pnpm --filter @kun-lang/docs dev",
    "docs:build": "pnpm --filter @kun-lang/docs build",
    "docs:preview": "pnpm --filter @kun-lang/docs preview",
    "docs:lint": "pnpm --filter @kun-lang/docs lint"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
