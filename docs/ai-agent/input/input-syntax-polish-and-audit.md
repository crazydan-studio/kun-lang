# 输入记录：语法打磨与全面审计

## 来源

项目维护者持续评审

## 日期

2026-06-04

## 原始指令

### 批次 1：语法操作符修订

将 `=?` 改为 `=!`，`<-?` 改为 `<-!`，不等号 `!=` 改为 `/=`，避免与 `?` 的语义发生冲突。

### 批次 2：四项设计改进评估

1. 支持针对 IO 效应定义无参函数 `\ ->`
2. 支持扩展积类型 `{ BaseType | field: T }`
3. 支持行多态积类型 `{ a | name: String } -> String`
4. 导入/List 解构/展开/模式匹配 `*` 改为 `..`

### 批次 3：领域补充文档

- 去除或替换 Maybe 相关设计和代码示例
- 修正 Kun 代码中的错误注释语法（`--` → `//`）
- 按变更意图分别提交

### 批次 4：多轮全面审计

对类型系统、语法设计、能力安全系统进行多轮审计，覆盖：
- Linux 脚本需求覆盖度
- 设计缺失、缺陷和漏洞
- 内容和表述冲突
- 已过时内容
- 非核心文档（audits/ process/ context/ backlog/ supply-chain）
- 元问题（README、版本历史、代码标签、讨论索引）

## 处理结果

- 批次 1-3：全部实施，文档已更新，git 按意图分 6 个提交
- 批次 4：8 轮审计共发现并修复 49 项问题，末轮确认零残留
- 设计文档截至本记录已全面自洽
