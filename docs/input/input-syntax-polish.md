# 输入记录：语法细节打磨

## 来源

项目维护者，2026-05-30

## 涉及变更

| # | 问题 | 决策 |
|---|------|------|
| 1 | 脚本文件后缀 | `.kun` |
| 2 | 优先级列表中含义不清 | `(expr)` 表示表达式分组，空格是分隔符 |
| 3 | 优先级说明用括号备注 | 改用 `//` 注释风格 |
| 4 | 反向管道示例多余参数 | `sqrt 4` 应为 `sqrt` |
| 5 | `checkFile'` 中 `'` 的含义 | `'` 是合法字符，无特殊语义 |
| 6 | `x = compute()` 非 Kun 语法 | 示例改为 `result = factorial 100` |
| 7 | 常量表达式不能体现惰性求值 | 改为 `result = factorial 100` |
| 8 | 运算符表将空格列为运算符 | 移除该行 |
| 9 | 表达式分组未列在运算符表 | 新增表达式分组行 |

## 设计结果

- `syntax.md`：全部修复
- `conventions.md`：新增 `.kun` 后缀约定
- 示例文件更新
